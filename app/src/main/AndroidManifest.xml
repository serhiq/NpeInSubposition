<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="ru.evotor.permission.receipt.print.INTERNET_RECEIPT" />
    <uses-permission android:name="ru.evotor.permission.receipt.printExtra.SET" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Example"
        tools:targetApi="31">

        <meta-data
            android:name="app_uuid"
            android:value="0abc3d6c-6817-4f96-98ff-3e1dbe7d21d0" />
        <activity
            android:name=".CrashReportActivity"
            android:exported="false"
            android:theme="@style/Theme.Example" />
        <activity
            android:name=".TerminalChecksActivity"
            android:exported="true">

            <meta-data
                android:name="ru.evotor.launcher.BACKGROUND_COLOR"
                android:value="#E91E63" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.EVOTOR" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <receiver
            android:name=".receivers.EventsReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="evotor.intent.action.receipt.sell.RECEIPT_CLOSED" />
                <action android:name="evotor.intent.action.receipt.payback.RECEIPT_CLOSED" />
                <action android:name="evotor.intent.action.receipt.buy.RECEIPT_CLOSED" />
                <action android:name="evotor.intent.action.receipt.buyback.RECEIPT_CLOSED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".services.ReceiptClosedService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />
    </application>
</manifest>
